#!/bin/sh
# .githooks/commit-msg — 编排：标题规范 + 强制 Signed-off-by + 生成 AI trailer
here="$(dirname "$0")"
# 先校验标题（fail fast，避免给不合规提交写 trailer），再补签名 / AI trailer
sh "$here/common/commit-title.sh" "$1"    || exit 1
sh "$here/common/signoff-trailer.sh" "$1" || exit 1
exit 0
